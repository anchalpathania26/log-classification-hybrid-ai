# 🚀 Hybrid Log Classification System

An intelligent log classification system that combines **Regex, Machine Learning, and LLMs** to handle logs of varying complexity with high accuracy and flexibility.

---

## 📌 Overview

Logs generated by real-world systems are often inconsistent and noisy. A single approach cannot effectively classify all types of logs.

This project introduces a **hybrid framework** that dynamically selects the best classification method:

* **Regex** → for simple and structured logs
* **ML (Sentence Transformer + Logistic Regression)** → for complex patterns with labeled data
* **LLM** → for ambiguous or low-data scenarios

---

## 🧠 Key Features

* Hybrid architecture (Rule-based + ML + LLM)
* Semantic understanding using embeddings
* Intelligent fallback using LLMs
* FastAPI-based API for real-time inference
* Scalable and modular design

---

## 🏗️ System Architecture

The following diagram illustrates how the hybrid classification pipeline dynamically selects the best approach based on log complexity:

![Architecture](resources/architecture.png)

### 🔍 Flow Explanation

1. **Log Input** → Incoming log message

2. **Regex Classification**

   * If matched → ✅ Direct classification (fast path)
   * If not matched → sent to next stage

3. **Decision Layer**

   * If sufficient labeled data exists → use ML model
   * Otherwise → fallback to LLM

4. **ML Classification (BERT / Sentence Transformer)**

   * Handles complex but learnable patterns

5. **LLM Classification**

   * Handles ambiguous or unseen patterns

👉 This hybrid flow ensures both **efficiency and robustness** in real-world scenarios.

---

## ⚙️ Tech Stack

* Python
* FastAPI
* Scikit-learn
* Sentence Transformers
* LLM APIs

---

## 📂 Project Structure

* `training/` → Model training scripts
* `models/` → Saved models
* `resources/` → Sample data
* `server.py` → API server

---

## 🚀 Getting Started

```bash
pip install -r requirements.txt
uvicorn server:app --reload
```

---

## 📥 Usage

Upload a CSV file with:

* `source`
* `log_message`

### Output:

Returns a CSV with:

* `target_label` (predicted class)

---

## 💡 Why This Project Matters

This project demonstrates:

* Real-world ML system design
* Handling unstructured data
* Combining multiple AI approaches
* Building deployable APIs

